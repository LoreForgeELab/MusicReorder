# MusicReorder

MusicReorder is a Python-based desktop utility built with Tkinter for organizing MP3 libraries. It features an intuitive drag-and-drop interface to manually sequence tracks. Users can batch-rename files with numeric prefixes based on their list position or strip existing numbers to clean up filenames, ensuring perfect playback order in any media player.
